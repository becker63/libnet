syntax = "proto3";

package libfuzzer_example;

import "google/protobuf/any.proto";

// A small message intended for quick fuzzing iterations.
message Msg {
  // optional presence fields (proto3 optional)
  optional string optional_string = 1;
  optional uint64 optional_uint64 = 2;
  optional float optional_float = 3;

  // allow arbitrary nested descriptors via Any to exercise UnpackTo etc.
  google.protobuf.Any any = 4;
}
